import random  def quickselect(arr, k, left=0, right=None):     if right is None:         right = len(arr) - 1      pivot_index = random.randint(left, right)     pivot_index = partition(arr, left, right, pivot_index)      if k == pivot_index:         return arr[k]     elif k < pivot_index:         return quickselect(arr, k, left, pivot_index - 1)     else:         return quickselect(arr, k, pivot_index + 1, right)  def partition(arr, left, right, pivot_index):     pivot_value = arr[pivot_index]     arr[pivot_index], arr[left] = arr[left], arr[pivot_index]     store_index = left      for i in range(left + 1, right + 1):         if arr[i] < pivot_value:             store_index += 1             arr[i], arr[store_index] = arr[store_index], arr[i]      arr[left], arr[store_index] = arr[store_index], arr[left]     return store_index